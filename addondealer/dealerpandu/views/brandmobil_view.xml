<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- dealerpandu.brandmobil tree view -->
    <record id="dealerpandu_brandmobil_view_tree" model="ir.ui.view">
        <field name="name">Brand Mobil</field>
        <field name="model">dealerpandu.brandmobil</field>
        <field name="arch" type="xml">
            <tree>
                <!-- Add your fields here -->
                <field name="name"/>
                <field name="founder_brand"/>
                <field name="brand_country"/> 
                <field name="jml_mobil"/>
                <field name="daftar"/>
            </tree>
        </field>
    </record>

    <!-- dealerpandu.brandmobil form view -->
    <record id="dealerpandu_brandmobil_view_form" model="ir.ui.view">
        <field name="name">Masukan Brand Mobil</field>
        <field name="model">dealerpandu.brandmobil</field>
        <field name="arch" type="xml">
            <form string="">
                <sheet>
                    <field name="logo_brand" widget="image" class="oe_avatar"/>
                    <group name="brand" string="Data Brand">
                        <!-- Add your fields here -->
                        <field name="name"/>
                        <field name="brand_country"/> 
                        <field name="founder_brand"/>
                        <field name="jml_mobil"/>
                    </group>
                    <group name="daftar" string="Daftar Mobil">
                        <field name="daftarmobil_ids"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- dealerpandu.brandmobil kanban view -->
    <!-- <record id="dealerpandu_brandmobil_view_kanban" model="ir.ui.view">
        <field name="name">Kanban View</field>
        <field name="model">dealerpandu.brandmobil</field>
        <field name="arch" type="xml">
            <kanban string="Kanban Description">
               
                <field name="logo_brand"/>
                <field name="name"/>
                <field name="brand_country"/> 
                <field name="founder_brand"/>
                <template id="template_kanban" name="kanban">
                    <t t-name="kanban-box">
                            <div class="oe_kanban_global_click o_kanban_record_has_image_fill o_dealerpandu_brandmobil_kanban">
                                <t t-if="!record.brandmobil.raw_value">
                                    <t t-set="background_image" t-value="kanban_image('dealerpandu', 'logo_brand', record.id.raw_value)"/>
                                    <div class="o_kanban_image_fill_left d-none d-md-block" t-attf-style="background-image:url('#{background_image}')">
                                        <img class="o_kanban_image_inner_pic" t-if="record.parent_id.raw_value" t-att-alt="record.parent_id.value" t-att-src="kanban_image('dealerpandu', 'logo_brand', record.brandmobil.raw_value)"/>
                                    </div>
                                    <div class="o_kanban_image d-md-none" t-attf-style="background-image: url(#{background_image})">
                                        <img class="o_kanban_image_inner_pic" t-if="record.parent_id.raw_value" t-att-alt="record.parent_id.value" t-att-src="kanban_image('dealerpandu', 'logo_brand', record.brandmobil.raw_value)"/>
                                    </div>
                                </t>
                                <t t-else="">
                                    <div class="o_kanban_image_fill_left o_kanban_image_full" t-attf-style="background-image: url(#{kanban_image('res.partner', 'avatar_128', record.id.raw_value)})" role="img"/>
                                </t>
                                <div class="oe_kanban_details d-flex flex-column">
                                    <strong class="o_kanban_record_title oe_partner_heading"><field name="display_name"/></strong>
                                    <div class="o_kanban_tags_section oe_kanban_partner_categories"/>
                                    <ul>
                                        <li t-if="record.parent_id.raw_value and !record.function.raw_value"><field name="parent_id"/></li>
                                        <li t-if="!record.parent_id.raw_value and record.function.raw_value"><field name="function"/></li>
                                        <li t-if="record.parent_id.raw_value and record.function.raw_value"><field name="function"/> at <field name="parent_id"/></li>
                                        <li t-if="record.city.raw_value or record.country_id.raw_value">
                                            <t t-if="record.city.raw_value"><field name="city"/><t t-if="record.country_id.raw_value">, </t></t>
                                            <t t-if="record.country_id.raw_value"><field name="country_id"/></t>
                                        </li>
                                        <li t-if="record.email.raw_value" class="o_text_overflow"><field name="email"/></li>
                                    </ul>
                                    <div class="o_row">
                                        <span class="oe_kanban_partner_links"/>
                                    </div>
                                </div>
                            </div>
                        </t>
                </template>
            </kanban>
        </field>
    </record> -->

    <!-- dealerpandu.brandmobil action window -->
    <record id="dealerpandu_brandmobil_action" model="ir.actions.act_window">
        <field name="name">Brand Mobil</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">dealerpandu.brandmobil</field>
        <field name="view_mode">tree,form,kanban</field>
    </record>

    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="dealerpandu_brandmobil_menu_act" 
              name="Brand Mobil" 
              parent="katalog_menu_categ" 
              action="dealerpandu_brandmobil_action" 
              sequence="10"/>

</odoo>
